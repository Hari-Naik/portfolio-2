import { Link } from "react-router-dom";

const Logo = () => {
  return (
    <div className="">
      <Link to="/">
        <h2 className="text-[1.4rem] text-[#ffd700] font-semibold">
          Harinaik.dev
        </h2>
      </Link>
    </div>
  );
};

export default Logo;
